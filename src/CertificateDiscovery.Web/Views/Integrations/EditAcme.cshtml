@model CertificateDiscovery.Contracts.AcmeProviderUpsertRequest
@{
    ViewData["Title"] = "Edit ACME Provider";
}

<div class="page-heading">
    <div>
        <h2>Edit ACME Provider</h2>
        <div class="text-muted">@Model.Name</div>
    </div>
    <a class="btn btn-outline-secondary btn-sm" asp-action="Index">Back</a>
</div>

@await Html.PartialAsync("_AcmeForm", Model)
