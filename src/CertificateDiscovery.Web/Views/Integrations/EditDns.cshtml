@model CertificateDiscovery.Contracts.DnsProviderUpsertRequest
@{
    ViewData["Title"] = "Edit DNS Provider";
}

<div class="page-heading">
    <div>
        <h2>Edit DNS Provider</h2>
        <div class="text-muted">Update DNS provider API settings.</div>
    </div>
    <a class="btn btn-outline-secondary btn-sm" asp-action="Index">Back</a>
</div>

@await Html.PartialAsync("_DnsForm", Model)
